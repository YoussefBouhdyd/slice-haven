# Run the app
`cd` to /slice-haven and the exeucte these commands
```
pip install -r requirements.txt
python3 app.py
```
> You can visite the website from [here](https://slice-haven.onrender.com/) (it may take a moment to load).
